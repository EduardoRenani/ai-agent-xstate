# Reflection Log

## 2026-05-14 — Initial generation

### C4

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| c1-context.mmd | pass | pass | pass | 1 |
| c2-container.mmd | pass | pass | pass | 1 |
| flows/user-chat.mmd | pass | pass | pass | 1 |

Notes:
- c1-context.mmd: frontmatter moved before `%%` comments (iteration 0 had parse error).
- C3 not generated: no module has >3 components with non-trivial orchestration.

### Behavioral

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| state-machines/agent.mmd | pass | pass | pass | 1 |

Notes:
- Frontmatter `---title---` block moved before `%%` comments (iteration 0 had parse error).
- No terminal state in the machine — agent runs indefinitely. Documented via `note` annotation.
- No `.md` companion: machine has <8 states and no complex guards.

## 2026-05-14 — Incremental: Atlas identity, per-state system prompts, PARTIALLY_RESPONDED

### C4

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| c1-context.mmd | pass (no content change) | — | — | 0 |
| c2-container.mmd | pass | pass | pass | 1 |
| flows/user-chat.mmd | pass | pass | pass | 1 |

Notes:
- c1-context.mmd: only header updated (modo: incremental). Structure unchanged.
- c2-container.mmd: interface #3 payload updated to `{ messages, systemPrompt }`.
- flows/user-chat.mmd: rewritten to show greetings invoke + PARTIALLY_RESPONDED flow.

### Behavioral

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| state-machines/agent.mmd | pass | pass | pass | 1 |

Notes:
- greetings changed from transient (always) to invoke state with raise(PARTIALLY_RESPONDED).
- idle transition now includes appendUserMessage action.
- listening handles both PARTIALLY_RESPONDED and MESSAGE events.

## 2026-05-14 — Incremental: actor naming convention (DD-008), structured output, conditional PARTIALLY_RESPONDED

### C4

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| c1-context.mmd | pass (no change) | — | — | 0 |
| c2-container.mmd | pass (no change) | — | — | 0 |
| c2-container.md | pass (no change) | — | — | 0 |
| flows/user-chat.mmd | pass | pass | pass | 1 |

Notes:
- flows/user-chat.mmd: actor names updated (`callLLM` → `greetingsNode`/`improviseThinkingNode`). greetingsNode now returns structured `{ greeting, needsFollowUp }`. PARTIALLY_RESPONDED shown as conditional.

### Behavioral

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| state-machines/agent.mmd | pass | pass | pass | 1 |

Notes:
- greetings onDone transition label updated to show conditional raise.
- Notes updated: greetingsNode returns structured output, PARTIALLY_RESPONDED only if needsFollowUp. thinking note references improviseThinkingNode.

## 2026-05-15 — Incremental: spec 003 agent modes + analytical evaluating

Trigger: spec 003 (`docs/specs/003-agent-modes.md`) implemented across commits 01fc536, 44f5067, ee1c7d5, plus uncommitted change making `socraticEvaluatingMode` purely analytical (returns `ModeOutput<undefined>`, no message side effects, no console output).

### C4

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| c1-context.mmd | pass | not inspected (topology unchanged) | pass | 1 |
| c2-container.mmd | pass | not inspected | pass | 1 |
| c2-container.md | pass (markdown) | — | pass | 1 |
| flows/user-chat.mmd | pass | not inspected | pass | 3 |

Notes:
- c1-context.mmd: source ref updated from `src/openrouter.ts` to `src/llm-client.ts`. Topology unchanged.
- c2-container.mmd: same source-ref fix. Node `openrouterClient` renamed to `llmClient` reflecting `src/llm-client.ts` rename and that the abstraction is generic chat (tools supported via spec 002). Interface #3 payload updated to include optional `tools`.
- c2-container.md: companion table reflects the rename + tools payload.
- flows/user-chat.mmd: full rewrite. Old version was anchored to obsolete states (`idle`, `improvise.listening`, `PARTIALLY_RESPONDED`) and obsolete actor names (`greetingsNode`, `improviseThinkingNode`). New version captures: classifier as routing hub, three intents (socratic / improvise / none), socratic retry loop described in `Note` block (mermaid sequenceDiagram does not allow nesting `loop` inside `alt`), `ModeOutput<undefined>` from evaluating to make the analytical contract explicit. L1 failed twice (nested `alt`, then nested `loop`-in-`alt`, then `;` in Note text parsed as statement separator) before passing.

### Behavioral

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| state-machines/agent.mmd | pass | not inspected | pass | 1 |

Notes:
- state-machines/agent.mmd: full rewrite. New root states: `listening`, `classifying`, `greetings`, `socratic`, `improvising`. Socratic is now a four-substate compound (`sTeaching → sListening → sEvaluating → sDone` with retry edge `sEvaluating → sTeaching`). Notes document: classifier first-message short-circuit, socraticEvaluatingMode analytical contract (no message mutation, no console output), socraticTeachingMode re-teach behavior on retry.
- Compound substate names prefixed (`gThinking`, `sTeaching`, etc.) to avoid potential collision with root state `listening` — mermaid stateDiagram-v2 scopes substate names but the prefixes also improve diagram legibility.

### Aggregate validation

- `docs/architecture/README.md`: index entries still match all artifacts (no new files added, no files deleted).
- No orphan `.mmd` files detected.
- No internal-ID conflicts between artifacts.

## 2026-05-21 — Incremental: spec 004 atlas wrapper + Zoe migration

Trigger: spec 004 (`docs/specs/004-xstate-agent-wrapper.md`) implemented across PR #5 (open, in review). Monorepo restructure moved `src/` → `examples/zoe/src/`, state files renamed `*.mode.ts` → `*.ts`, agent renamed Atlas → Zoe (Atlas is now the library), state machine compiled via `packages/atlas/`.

### C4

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| c1-context.mmd | pass | pass (topology unchanged) | pass | 1 |
| c2-container.mmd | pass | pass (topology unchanged) | pass | 1 |
| c2-container.md | pass (no change) | — | — | 0 |
| flows/user-chat.mmd | pass | pass (structure unchanged) | pass | 1 |

Notes:
- c1-context.mmd, c2-container.mmd: source refs rebased onto `examples/zoe/src/`. c2 source ref adds parenthetical `(state machine compiled via packages/atlas/)` to flag the new build-time dependency. Runtime topology unchanged — atlas is a library, not a container.
- flows/user-chat.mmd: source refs rebased + `.mode.ts` → `.ts` rename. Sequence structurally unchanged (same five participants, same classifier-dispatch flow, same socratic retry loop).
- c2-container.md companion: no edit (interfaces unchanged).

### Behavioral

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| state-machines/agent.mmd | pass | pass (structure unchanged) | pass | 1 |

Notes:
- agent.mmd: title `Atlas Agent` → `Zoe Agent` (agent rename). Source refs rebased + parenthetical `(built with packages/atlas/)`. State topology unchanged — atlas compiles the same five root states (listening, classifying, greetings, socratic, improvising) from declarative inputs. The four-outcome contract (`achieved` / `retry` / `abandoned` / `error`) atlas formalizes is the same behavior already captured in the diagram via the socratic retry loop and improvising onError edge.

### Aggregate validation

- `docs/architecture/README.md`: refreshed to name Zoe explicitly + accurate state set (was stale: `idle, greetings, improvise`).
- No orphan `.mmd` files detected.
- No internal-ID conflicts.

## 2026-05-22 — Incremental: spec 005 (deps + JSON context), spec 006 (states→modes), DD-024 (Mode/CompoundMode rename)

Trigger: three atlas-internal landings since the prior run — `ac95c75` (spec 005: `deps` channel + `JsonCompatible<TContext>`), `d9eefbe` (spec 006: `states` → `modes` field rename), `ae42fa7` (DD-024: `LeafMode`/`Mode` → `Mode`/`CompoundMode` vocabulary refinement). Each touches the atlas wrapper's authoring surface and type contract; none changes Zoe's runtime topology, container set, or state machine.

### C4

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| c1-context.mmd | pass (no change) | — | — | 0 |
| c2-container.mmd | pass (no change) | — | — | 0 |
| c2-container.md | pass (no change) | — | — | 0 |
| flows/user-chat.mmd | pass (no change) | — | — | 0 |

Status: **no_op**. Reason: features contained in `packages/atlas/` (declarative wrapper). The container topology (CLI → State Machine → LLM Client → OpenRouter) is unaffected; spec 005's `deps` channel is a wrapper capability that Zoe does not exercise (Zoe imports `llm-client` directly rather than threading it via `defineAgent({ deps })`). Sequence flow unchanged — classifier dispatch, socratic retry, improvising tools loop all identical.

### Behavioral

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| state-machines/agent.mmd | pass (no change) | — | — | 0 |

Status: **no_op**. Reason: Zoe's state set (`listening`, `classifying`, `greetings`, `socratic`, `improvising`) and every transition (MESSAGE → classifying, intent dispatch, socratic retry edge, improvising onError → END) match the diagram. The wrapper-vocabulary changes (`states` → `modes`, `LeafMode`/`Mode` → `Mode`/`CompoundMode`) do not surface in the behavioral view, which speaks in state names and triggers, not in the constructor names that produce them.

### Aggregate validation

- `docs/architecture/README.md`: index entries still match all artifacts on disk (no creations, no deletions).
- No orphan `.mmd` files detected.
- No internal-ID conflicts.

## 2026-05-28 — Incremental: flatten greetings + improvising (compound → leaf)

Trigger: flatten of single-substate one-shot modes, PR #14 / branch `feat/flatten-single-substate-modes` (commits `714ccca`, `f9b3107`). `greetings` and `improvising` each wrapped a single `*.thinking` substate with no MESSAGE-handling child; both collapsed into leaf `defineMode`s. Source: `examples/zoe/src/states/greetings.ts`, `examples/zoe/src/states/improvising.ts` (DD-026).

### C4

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| c1-context.mmd | pass (no change) | — | — | 0 |
| c2-container.mmd | pass (no change) | — | — | 0 |
| c2-container.md | pass (no change) | — | — | 0 |
| flows/user-chat.mmd | pass | pass | pass | 1 |

Notes:
- c1-context / c2-container / c2-container.md: container topology unchanged (User → CLI → State Machine → LLM Client → OpenRouter). Flatten is internal to the state machine, no impact on the C4 static/interface views.
- flows/user-chat.mmd: greetings/improvising notes de-nested — `classifying → greetings.thinking` → `classifying → greetings`, `greetingsThinkingMode` → `greetingsMode`, dropped the `→ greetings.done`/`→ improvising.done` hops; same for improvising. Date bumped to 2026-05-28. An error-recovery note added during manual edits was **removed** here to comply with the parent's happy-path-only rule (§1, §10) — sequence flows do not depict transport errors. Rendered to PNG, visually inspected: five participants, no overlap, loop exit condition intact.

### Behavioral

| Artifact | L1 Syntax | L2 Visual | L3 Semantic | Iterations |
|---|---|---|---|---|
| state-machines/agent.mmd | pass | pass | pass | 1 |

Notes:
- agent.mmd: `state greetings { gThinking → gDone }` and `state improvising { iThinking → iDone }` compound blocks collapsed into leaf transitions `greetings --> classifying: onDone [achieved] / assign messages, print` and `improvising --> classifying: onDone [achieved] / assign messages, print`. The improvising `onError → classifying / assign messages, log error` edge is retained (agent abort/recovery transition per behavioral-doc §9.2; consistent with prior runs) and now reflects the post-flatten recover-and-log behavior. Nesting depth drops to 1 (only `socratic` remains compound). Date bumped to 2026-05-28. Rendered to PNG, inspected: leaf states flat, socratic compound intact, notes legible.

### Aggregate validation

- `docs/architecture/README.md`: index entries still match all artifacts on disk (no creations, no deletions — `.thinking.ts` removals were code files, not `.mmd`). Artifact descriptions still accurate (state set and flow names unchanged).
- No orphan `.mmd` files detected.
- No internal-ID conflicts (`gThinking`/`gDone`/`iThinking`/`iDone` IDs removed; remaining IDs unique).
